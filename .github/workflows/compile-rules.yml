name: Convert TXT to MRS
on:
  push:
    paths:
      - 'rules/**.txt' # 监听 txt 文件的变动
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download Mihomo
        run: |
          # 1. 下载并安装内核
          curl -L https://github.com/MetaCubeX/mihomo/releases/download/v1.19.20/mihomo-linux-amd64-compatible-v1.19.20.gz | gunzip > mihomo
          chmod +x mihomo

      - name: Convert TXT to MRS
        run: |
          # 2. 遍历 rules 目录下所有的 txt 文件，自动转换为 mrs
          find rules/ -name "*.txt" | while read file; do
            if [ -f "$file" ]; then
              filename="${file%.*}"
              echo "正在转换: $file -> ${filename}.mrs"
              
              # 注意这里：格式参数从 yaml 换成了 text
              ./mihomo convert-ruleset classical text "$file" "${filename}.mrs"
            fi
          done

      - name: Commit and Push
        run: |
          # 3. 提交转换后的二进制文件
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add rules/*.mrs
          
          if git diff --staged --quiet; then
            echo "没有变化，跳过提交。"
          else
            git commit -m "Auto-convert TXT to MRS"
            git push
          fi
